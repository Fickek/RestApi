-- create TABLE person(
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(255),
--     age INTEGER
-- );

create TABLE person(
    id SERIAL PRIMARY KEY,
    mail VARCHAR(255),
    password VARCHAR(255)
);